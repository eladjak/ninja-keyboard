# CI Status — 2026-06-14

- Run `27491617229` (`b94350a`): Lint+Type ✓, Unit ✓ (1324/1324), Build ✓, **E2E ✗** (195 passed / 8 flaky / 1 failed, 0 hydration errors).
- Green: all hydration errors eliminated (Problem 1); settings `25%` volume test now passes (Problem 2); E2E failures cut 214 → 1 by scoping CI to the installed Chromium browser.
- Red: one pre-existing **timing-flake** test — `lessons-flow.spec.ts:133 (results modal shows after lesson completion)`; the lesson stat renders ~1.5 s after store rehydration, past the test's instant assertion. Not caused by these changes (fails when fully reverted; only ever passed on Chromium by timing luck).
- Recommendation: add an explicit wait (`expect(getByTestId('lesson-stars-lesson-01')).toBeVisible()` before the `'10 מ/ד'` assertion), or migrate the E2E job to a production server (`next build && next start`) to remove dev-server compile latency that drives this flake class.
- Push budget (3) exhausted; stopping per the bounded rule. See `docs/CI-BLOCKER.md` for full detail.
